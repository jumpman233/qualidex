# Qualidex 开发基本原则

## 1. 产品原则

- 原始资料只读，不移动、不覆盖、不删除。
- SQLite 是结构化事实源，归档目录只是生成结果。
- AI 只做辅助抽取、建议和解释，不做最终裁决。
- 低置信度、冲突和失败项必须进入人工确认流程。
- 优先跑通闭环，再扩展复杂能力。

## 2. 技术原则

- 桌面端使用 Electron + electron-vite + React + TypeScript。
- Main Process 负责本地文件、数据库、系统能力。
- Renderer 负责界面展示和用户操作。
- Renderer 不直接访问 Node 文件系统能力，统一通过 preload IPC。
- IPC 接口使用明确的请求/响应类型，错误返回结构化信息。

## 3. 代码原则

- TypeScript 开启严格类型检查。
- 业务逻辑优先放在可测试的 service 层，不写死在 UI 组件里。
- 文件、数据库、OCR、AI 等外部能力通过 adapter/service 抽象。
- 先使用简单清晰的模块边界，避免过早抽象。
- 命名使用英文，页面文案使用中文。

## 4. 数据与安全原则

- 不上传原图、身份证图片、学历证书图片等原始资料到云端 AI。
- 发送给 AI 的内容只能是 OCR 后文本，并尽量脱敏。
- 删除默认软删除，保留恢复能力。
- 关键修改、删除、导出操作需要记录审计日志。

## 5. P0 执行原则

- P0 只验证桌面壳和基础工程链路。
- 暂不实现数据库、OCR、AI、扫描和归档。
- 每完成一个小块就运行一次验证命令。
- 不引入与 P0 无关的大型依赖。
